version: '3.8'

services:
  gopay-gateway:
    build: .
    container_name: gopay-gateway
    restart: always
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
